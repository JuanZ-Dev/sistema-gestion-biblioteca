{% extends 'base.html.twig' %}

{% block title %}Autor{% endblock %}

{% block body %}
    <h1>Autor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ autor.nombre }}</td>
            </tr>
            <tr>
                <th>Nacionalidad</th>
                <td>{{ autor.nacionalidad }}</td>
            </tr>
            <tr>
                <th>Fecha de nacimiento</th>
                <td>{{ autor.fechaNacimiento ? autor.fechaNacimiento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fecha de fallecimiento</th>
                <td>{{ autor.fechaFallecimiento ? autor.fechaFallecimiento|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_autor_index') }}">back to list</a>

    <a href="{{ path('app_autor_edit', {'id': autor.id}) }}">edit</a>

    {{ include('autor/_delete_form.html.twig') }}
{% endblock %}
